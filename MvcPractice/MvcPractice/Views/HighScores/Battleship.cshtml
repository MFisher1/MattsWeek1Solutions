@model IEnumerable<MvcPractice.Models.HighScore>
@{
    ViewBag.Title = "Battleship";
}

<h2>Battleship</h2>

<ol>
    @foreach (var item in Model)
    {
        <li>
            @item.Name-@item.Date.Value.ToShortDateString() <p>Score: @item.Score</p> 
        </li>
    }
</ol>
